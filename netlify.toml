[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"

[functions]
  directory = "frontend/.netlify/functions-internal"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
